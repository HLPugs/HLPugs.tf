import { PermissionGroup } from './PermissionGroup';

export enum Permission {
	BAN_PLAYER,
	MODIFY_ROLE
}

// type permissionMap = Record<Permission, PermissionGroup[]>;
// const permissionsMap: Permission
// 	BAN_PLAYER: [PermissionGroup.ADMIN, PermissionGroup.MODERATOR]
// };