{
  "name": "@hlpugs/common",
  "version": "0.1.0",
  "main": "lib/index.js",
  "private": true,
  "scripts": {
    "build": "rimraf dist && npm run build-ts",
    "build-ts": "tsc -p tsconfig.json",
    "dev": "npm run build && npm run watch-ts",
    "watch": "tsc -w",
    "tslint": "tslint -c tslint.json -p tsconfig.json"
  },
  "dependencies": {
    "rimraf": "^3.0.0"
  }
}
